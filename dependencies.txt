# Dependencies:

# this is where ikura resolves his dependencies
# if needed you can edit the following paths according to your system configuration

PYPATH=$(dirname $(which python3.6))
RPATH=$(dirname $(which R))

FASTQC=fastqc
SALMON=salmon
